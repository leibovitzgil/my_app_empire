name: template_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  core_ui:
    path: ../../packages/core/core_ui

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^5.1.0
