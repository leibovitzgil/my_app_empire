rules_version = '2';

// NOTE: permissive rules for local development / the emulator. Lock these down
// before any production deploy (e.g. require auth and restrict to household
// members).
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
