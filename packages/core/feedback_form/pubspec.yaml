name: feedback_form
description: "Shared feedback form for collecting user ratings and comments."
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  core_ui:
    path: ../core_ui
  core_utils:
    path: ../core_utils
  equatable: ^2.0.5
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.3.0
