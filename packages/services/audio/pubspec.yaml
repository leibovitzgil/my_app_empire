name: audio
description: "Recording and playback services for review audio notes."
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  clock: ^1.1.1
  core_utils:
    path: ../../core/core_utils
  equatable: ^2.0.8
  flutter:
    sdk: flutter
  just_audio: ^0.10.6
  record: ^7.1.1

dev_dependencies:
  fake_async: ^1.3.1
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.3.0
